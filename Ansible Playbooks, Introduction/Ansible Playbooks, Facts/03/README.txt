use the following command to only limited the query to certain hosts

## la paramétre l est une abbreviations a le mot limit que caut dire que c'est on just une seul que nous inétressé on peut le mentioné ici
ansible-playbook <playbook> -l 
